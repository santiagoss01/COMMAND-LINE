cd thecmdchallenge/ (Comando usado para ingresar al directiorio)
dirs (comando usado para imprimir el path del directorio actual)
ls -a (commando usado para listar todos los archivos del directorio incluidos los ocultos)
ls -R (comando usado para listar de manera recursiva)
clear (comando usado para limpiar el terminal)

find . -name "trophy.txt"
cd (nombre del directorio producido por la busqueda)
cat (para imprimir el archivo trophy.txt)

cd .. (para subir al directorio padre)
cd (para ingresar en el directorio Funcode)
ls *.js (para listar los archivos con extensión .js)

cd (para ir al directorio the-most-funny)
mkdir (para crear el directorio not that funny)
ls (para verificar que se ha creado)

find . -name(para encontrar el archivo en el directorio)
cp (para copiar el archivo)

find. -name (para encontrar el path del archivo)
mv (para mover el archivo entre un subdirectorio y otro)

rm -r (Para remover el directorio)

find . -name "the-ultimate-joke.txt"
cd (nombre del directorio producido por la busqueda)
cat (para imprimir el archivo the-ultimate-joke.txt)

rm -r(remover boringfolder)

cd (para ir al directorio que contiene el archivo)
vi + nombre del archivo
x (para borrar dentro del editor de texto vi)
:wq (para guardar los cambios y salir.)



